import {ScaledSheet} from 'react-native-size-matters';

export const styles = ScaledSheet.create({
  headerTextStyle: {
    fontSize: '40@ms',
    color: 'white',
    position: 'absolute',
    paddingBottom: '5@ms',
    left: 0,
    bottom: 0,
  },
});
